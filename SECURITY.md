# 安全政策

## 支持范围

安全修复以当前 `main` 分支为准。较早版本可能不会单独回补。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下报告安全问题。不要创建公开 Issue，也不要附带真实用户数据或仍然有效的生产凭据。

报告中请包括受影响的组件、复现条件、潜在影响和建议修复方式。维护者会尽快确认收到，并在完成评估后协调披露时间。

## 凭据处理

微信 AppSecret、腾讯云 SecretId/SecretKey、维护密钥、后台密码和用户 openid 都不得进入版本控制。若凭据曾被提交，应立即在对应平台轮换；仅删除文件或重写 Git 历史不足以使凭据失效。
