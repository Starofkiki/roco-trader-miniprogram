# 参与贡献

感谢你愿意改进远行商人记录本。

## 提交问题

请先搜索已有 Issue，并尽量说明：

- 出现问题的页面或功能
- 复现步骤和预期行为
- 微信版本、系统版本和小程序版本
- 已去除 openid、密钥、昵称等信息的截图或日志

不要在公开 Issue 中提交 AppSecret、腾讯云密钥、维护密钥、openid、数据库导出或未经打码的后台截图。安全问题请改用 `SECURITY.md` 中的私下渠道。

## 提交代码

1. 从 `main` 创建功能分支。
2. 每个 Pull Request 只处理一个明确问题，避免顺带重构无关代码。
3. 保持现有 JavaScript、WXML 和 WXSS 风格。
4. 为数据解析、缓存、统计或提醒逻辑补充相应的 Node.js 回归测试。
5. 在提交前运行与改动相关的 `tools/**/test-*.js` 脚本及 `node --check`。
6. 在 Pull Request 中写明改动、验证方法，以及是否影响云数据库集合或环境变量。

涉及真实微信消息、生产集合写入或付费云资源的测试，必须由维护者明确确认后进行。

## 数据与素材

贡献内容必须有权公开和再许可。不要提交生产数据、用户数据、第三方私有接口响应、抓取到的受限内容或来源不明的素材。
